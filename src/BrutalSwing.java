import ru.ifmo.se.pokemon.StatusMove;
import ru.ifmo.se.pokemon.Type;

public class BrutalSwing extends StatusMove {
    public BrutalSwing(double pow, double acc) {
        super(Type.DARK, pow, acc);
    }
}
