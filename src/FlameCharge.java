import ru.ifmo.se.pokemon.StatusMove;
import ru.ifmo.se.pokemon.Type;

public class  FlameCharge extends StatusMove {
    public FlameCharge(double pow, double acc) {
        super(Type.FIRE, pow, acc);
    }
}
